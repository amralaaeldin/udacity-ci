{
    "scripts": {
        "complete:install": "cd complete && npm install",
        "server:install": "cd server && npm install",
        "complete:build": "cd complete && ls && npm run build",
        "server:build": "cd server && ls && npm run build"
    }
}